import { ErrorMessageWrapper } from "./styles"

export const ErrorMessage = () => {
  return (
    <ErrorMessageWrapper className="high-emphasis">
      Failed to Load Data
    </ErrorMessageWrapper>
  )
}
